import { NavLink, Outlet } from "react-router-dom"

export default function Login () {
    return (
        <div>
            HOLISS LOGINSS
            <NavLink to="/">Home</NavLink>
        </div>
    )
}